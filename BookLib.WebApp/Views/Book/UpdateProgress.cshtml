@model BookLib.WebApp.Models.BookInfoViewModel
@{
    ViewData["Title"] = "Enter the name of a book and where you have finished reading";
}
<h1>@ViewData["Title"]</h1>

<div class="row">
    <div class="col-md-4">
        <form asp-action="UpdateProgress">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label class="control-label">Name of a book</label>
                <select class="form-control" name="bookname">
                @{
                    foreach (var item in Model.Books)
                    {
                                    <option> @Html.DisplayFor(modelItem => item.BookName)</option>
                    }
                }
                </select>
                <span asp-validation-for="BookName" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label class="control-label">Page on which you have finished reading</label>
                <input class="form-control" name="finishpage" />
                <span asp-validation-for="FinishPage" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Update" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>
<div>
    <a asp-controller="Home" asp-action="Index">Back to List</a>
</div>